# Static Website 4 23degree.org
## we feel the world
### yeah

Installing

` npm install && bower install `

Local live preview

` gulp serve `

Build

` gulp build `

Nicht alle Verzeichnisse werden mittels 'build' in den dist-Ordner kopiert
